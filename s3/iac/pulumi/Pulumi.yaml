name: s3-simple-sdk
description: Python project for AWS S3 to create a bucket.
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
